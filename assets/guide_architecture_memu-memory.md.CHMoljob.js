import{_ as i,o as a,c as e,ag as n}from"./chunks/framework.ePeAWSvT.js";const m=JSON.parse('{"title":"memU 记忆（CLI，实验性）","description":"","frontmatter":{},"headers":[],"relativePath":"guide/architecture/memu-memory.md","filePath":"guide/architecture/memu-memory.md"}'),t={name:"guide/architecture/memu-memory.md"};function l(p,s,o,r,h,d){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="memu-记忆-cli-实验性" tabindex="-1">memU 记忆（CLI，实验性） <a class="header-anchor" href="#memu-记忆-cli-实验性" aria-label="Permalink to &quot;memU 记忆（CLI，实验性）&quot;">​</a></h1><p>本页描述 <strong>memU</strong> 作为<strong>与 wikimem 并列的、可选的第二记忆后端</strong>如何集成。定位与决策见 <a href="/adr/0003-memu-cli-integration.html">ADR-0003</a>（及其前身 <a href="/adr/0002-memu-design-not-dependency.html">ADR-0002</a>）。</p><div class="warning custom-block"><p class="custom-block-title">实验性 · 默认关闭</p><p>memU 后端默认不启用，需要 <strong>Rust 工具链</strong>、可由 uv 解析的 <strong>Python 3.13</strong>，以及一个 <strong>OpenAI 兼容 embedding 端点</strong>（硬前置）。默认记忆后端仍是 <a href="https://wikimem.xnnehang.top/zh/" target="_blank" rel="noreferrer">wikimem</a>。</p></div><h2 id="上游形态-两个集成面-memu-adr-0008" tabindex="-1">上游形态：两个集成面（memU ADR-0008） <a class="header-anchor" href="#上游形态-两个集成面-memu-adr-0008" aria-label="Permalink to &quot;上游形态：两个集成面（memU ADR-0008）&quot;">​</a></h2><p>上游有<strong>两个分层的集成面</strong>，共享同一 store，可自由混用：</p><ul><li><strong>Surface A —— zero-code hooks（<code>memu-cli</code>）</strong>：面向 drop-in 用户；hook 契约 = <code>on_turn</code> （回合后异步记录）+ <code>on_prompt</code>（生成前同步注入）。本项目当前接的就是这一面的语义。</li><li><strong>Surface B —— programmatic API（<code>memu-py</code>）</strong>：面向把 memU 内嵌进自己 agent 的 builder， 代码里直接调 retrieve / memorize。<code>memu-py</code> 正在经历 trajectory-as-source <strong>大重构，尚未 就绪</strong>——就绪后本项目会<strong>再接一条 PY 连接</strong>（双面并存，不二选一），同样从子模块源码构建。</li><li>当前 CLI 面：<strong>memU 不做 LLM 抽取</strong>——<code>memu commit</code> 持久化宿主准备好的 recall files （memory track 按行切 segment，<code>#</code> 标题与空行跳过）；<code>memu retrieve</code> 是 0 LLM 的 embedding 检索，stdout 输出 JSON。要求 Python 3.13 + maturin/Rust，本项目（3.11）无法同进程导入。</li></ul><h2 id="架构" tabindex="-1">架构 <a class="header-anchor" href="#架构" aria-label="Permalink to &quot;架构&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Python 3.11 主进程 (lab)                     短生命周期 Python 3.13 子进程（uv 拉起，环境有缓存）</span></span>
<span class="line"><span>┌───────────────────────────────────┐</span></span>
<span class="line"><span>│ src/lab/plugins/memu (HookPlugin)  │ spawn  ┌─────────────────────────────────────┐</span></span>
<span class="line"><span>│ on_before_turn ───────────────────────────&gt; │ uv run --isolated --no-project       │</span></span>
<span class="line"><span>│   memu retrieve &quot;&lt;query&gt;&quot; → 注入   │        │   --python 3.13 --with packages/memU │</span></span>
<span class="line"><span>│ on_after_turn（后台任务）:          │        │   memu retrieve / memu commit -      │</span></span>
<span class="line"><span>│   抽取 LLM（继承 chat）             │        │ env: MEMU_DB / MEMU_EMBED_* /        │</span></span>
<span class="line"><span>│   → 影子 markdown 追加行            │        │      MEMU_CONFIG_ENV（私有）          │</span></span>
<span class="line"><span>│   → memu commit -（stdin JSON）    │        └─────────────────────────────────────┘</span></span>
<span class="line"><span>│ fail-open + 超时；无常驻进程        │        store: &lt;memory_dir&gt;/memu.sqlite3</span></span>
<span class="line"><span>└───────────────────────────────────┘</span></span>
<span class="line"><span>        影子记忆文件（事实源）: &lt;memory_dir&gt;/recall/&lt;category&gt;.md</span></span></code></pre></div><ul><li><strong>读</strong>（<code>on_before_turn</code>）：<code>memu retrieve</code> 拿 segments，按 <code>budget_tokens</code> 注入 <code>- 行内容</code>；超时/失败即本轮不注入（fail-open）。</li><li><strong>写</strong>（<code>on_after_turn</code>，后台）：插件自己做单次 LLM 抽取（与 wikimem 同一套 category/name/content 契约、同样默认继承 <code>lab.toml</code> chat 配置），条目以 <code>name：content</code> 行追加到影子 markdown，然后把<strong>整个文件</strong> <code>memu commit</code>——memU 按行 diff 重建 segments， 未变的行保留 embedding。</li><li><strong>影子文件是事实源</strong>：人可读可改、可 diff 可回滚；memU 的 SQLite 只是索引投影，随时可由 影子文件重建（逐个重新 commit 即可）。</li><li><strong>store 隔离</strong>：<code>MEMU_DB</code>、<code>MEMU_CONFIG_ENV</code> 都指到 <code>memory_dir</code> 下，绝不与用户全局 <code>~/.memu/config.env</code> 串味。</li><li>首次调用触发 maturin 源码构建（数分钟，uv 缓存后续复用）；构建期间召回 fail-open。</li></ul><h2 id="与-wikimem-的取舍" tabindex="-1">与 wikimem 的取舍 <a class="header-anchor" href="#与-wikimem-的取舍" aria-label="Permalink to &quot;与 wikimem 的取舍&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>wikimem（默认）</th><th>memU（实验，CLI）</th></tr></thead><tbody><tr><td>进程</td><td>同进程（uv workspace 成员）</td><td>每次调用一个 Python 3.13 短生命周期子进程</td></tr><tr><td>存储</td><td>扁平 markdown 分类文件</td><td>影子 markdown（事实源）+ SQLite（索引）</td></tr><tr><td>抽取</td><td>插件内单次 LLM</td><td>插件内单次 LLM（同一契约；memU 自身 0 LLM）</td></tr><tr><td>召回</td><td>BM25 + 可选 embedding 融合，毫秒级</td><td>embedding 检索，秒级（子进程 + embedding HTTP）</td></tr><tr><td>关联</td><td>内容内 <code>[[wiki-link]]</code></td><td>无</td></tr><tr><td>前置</td><td>无额外前置</td><td>Rust + Python 3.13 + embedding 端点（必须）</td></tr></tbody></table><p>两者可单独或同时启用；同时启用时两套记忆各自注入，适合做<strong>对照评测</strong>。</p><h2 id="启用-opt-in" tabindex="-1">启用（opt-in） <a class="header-anchor" href="#启用-opt-in" aria-label="Permalink to &quot;启用（opt-in）&quot;">​</a></h2><p>在某个 profile 里把 <code>memu</code> 加进 <code>[plugins] enabled</code>，并配 <code>[plugins.memu]</code>：</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">plugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">enabled = [</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # ... 既有插件 ...</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;memu&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">plugins</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">memu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">memory_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;memu_memory&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # memU 状态目录（相对 workspace_root）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">search_limit = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">budget_tokens = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">800</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 抽取 LLM：留空 = 继承 lab.toml 的 chat 模型配置</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">extraction_base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">extraction_model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">extraction_api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># embedding（OpenAI 兼容）：必填，留空则插件禁用</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">embedding_base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://your-embed-endpoint/v1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">embedding_model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;text-embedding-3-small&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">embedding_api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-...&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 子进程</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">python_version = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;3.13&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">retrieve_timeout = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # 召回超时（超时本轮不注入）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">commit_timeout = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # 写入超时（首次含源码构建，需宽松）</span></span></code></pre></div><p>全部配置项见 <code>src/lab/plugins/memu/plugin.toml</code> 的 <code>config_schema</code>。</p><h2 id="本地-smoke-无需任何外部-key" tabindex="-1">本地 smoke（无需任何外部 key） <a class="header-anchor" href="#本地-smoke-无需任何外部-key" aria-label="Permalink to &quot;本地 smoke（无需任何外部 key）&quot;">​</a></h2><p>用一个本地假 embedding 端点即可端到端验证 commit → retrieve（也是排障手段）。</p><ol><li>起一个确定性的假 <code>/v1/embeddings</code>（stdlib 即可，向量由文本哈希导出）。</li><li>环境变量指向它并选好 store：</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMU_BASE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://127.0.0.1:8901/v1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMU_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fake</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMU_EMBED_PROVIDER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">openai</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MEMU_DB</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">./.memu-smoke/memu.sqlite3</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;recall_files&quot;: [{&quot;name&quot;: &quot;preferences&quot;, &quot;track&quot;: &quot;memory&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;description&quot;: &quot;test&quot;, &quot;content&quot;: &quot;# preferences\\n手冲咖啡：只喝手冲，不加糖\\n&quot;}]}&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --isolated</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-project</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3.13</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --with</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./packages/memU</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> memu</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> commit</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> -</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --isolated</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --no-project</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3.13</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --with</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./packages/memU</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> memu</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> retrieve</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;咖啡偏好&quot;</span></span></code></pre></div><p>retrieve 的 JSON 里应能看到 <code>segments[].text = &quot;手冲咖啡：只喝手冲，不加糖&quot;</code> 带相似度分数 （本 round-trip 已在 pin <code>f51673e</code> 上验证通过）。首次运行会先做 maturin 构建（需要 Rust）。 热路径实测 <strong>2.1–2.4 s/次</strong>（Windows，本地假 embedding；真实端点再加一次 embedding HTTP 往返）。</p><div class="tip custom-block"><p class="custom-block-title">曾有的上游回归（已解决）</p><p>2026-07-16 集成时发现上游 embed 契约回归（<a href="https://github.com/NevaMind-AI/memU/issues/499" target="_blank" rel="noreferrer">memU#499</a>）， 上游当日以 <a href="https://github.com/NevaMind-AI/MemU/pull/504" target="_blank" rel="noreferrer">#504</a> 修复；re-pin 至 <code>f51673e</code> 后 round-trip 已验证。这也是本集成「contributor dogfooding 回路」的首个产出。</p></div><h2 id="re-pin-流程-升级子模块时" tabindex="-1">re-pin 流程（升级子模块时） <a class="header-anchor" href="#re-pin-流程-升级子模块时" aria-label="Permalink to &quot;re-pin 流程（升级子模块时）&quot;">​</a></h2><ol><li><code>git -C packages/memU fetch origin &amp;&amp; git -C packages/memU checkout &lt;新 commit&gt;</code>，父仓 <code>git add packages/memU</code></li><li><strong>必须让 uv 重建</strong>：uv 对路径源的缓存只看 <code>pyproject.toml</code> 的 mtime——版本号没变时 re-pin 会 <strong>静默复用旧构建</strong>（<code>--refresh-package</code> / <code>uv cache clean</code> 都不解）。插件已内置 fresh-build guard 自动处理；<strong>手动跑 CLI 时</strong>需 <code>touch packages/memU/pyproject.toml</code></li><li>跑上面的 keyless smoke 验证 commit → retrieve round-trip</li></ol><h2 id="已知代价" tabindex="-1">已知代价 <a class="header-anchor" href="#已知代价" aria-label="Permalink to &quot;已知代价&quot;">​</a></h2><ul><li>每次召回付一次子进程成本（实测 2.1–2.4 s，慢于 wikimem 的毫秒级）；根治路径是未来的 <strong>memu-py（Surface B）连接</strong>（见 <a href="/adr/0003-memu-cli-integration.html">ADR-0003</a>），届时双面并存。</li><li>首次启动有 Rust 构建延迟；构建期间召回不注入（fail-open）。</li><li><strong>uv 路径源静默缓存坑</strong>：re-pin 后版本号没变时 uv 会复用旧构建（见上方 re-pin 流程）； 插件已内置 guard，手动 CLI 需自己 touch。</li><li>承接 memU 的 Beta 抖动：下一个<strong>已宣告的破坏性变更</strong>是 CLI 命令收敛（<code>memorize</code>/<code>retrieve</code> 成为唯一命令对，<code>commit</code>/<code>list-files</code> 退场，无弃用期），届时 re-pin 需同步适配插件。</li></ul>`,26)])])}const c=i(t,[["render",l]]);export{m as __pageData,c as default};
